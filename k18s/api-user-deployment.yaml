apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: backend-user
  name: backend-user
spec:
  replicas: 2
  selector:
    matchLabels:
      service: backend-user
  template:
    metadata:
      labels:
        service: backend-user
    spec:
      containers:
      - image: abogalambo/udagram-api-user:v1
        name: backend-user
        ports:
        - containerPort: 8080
        imagePullPolicy: Always
        envFrom:
          - secretRef:
              name: env-secret
              optional: false
          - configMapRef:
              name: env-configmap
              optional: false